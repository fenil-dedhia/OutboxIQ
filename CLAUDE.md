# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

> **Reading note.** This file holds *current state + locked decisions*. The *why* behind trajectory-changing decisions lives in `notes/owner-decisions-log.md` (referenced here as "Entry N"); per-session history lives in `notes/session-N-summary.md`. Don't duplicate that reasoning back into here — point to it.

> **Brand + naming history (Entry 41, 2026-05-19).** Developed as **"OutboxIQ"** (Sessions 1–9; Entry 1 set the name, Entry 30 hardened brand-independent identifiers ahead of a possible rename), renamed to **"Fashionably Late"** for pre-launch positioning. Forward-looking docs, code, and user-facing strings use **"Fashionably Late"**; historical session summaries, owner-decisions-log Entries 1–40, and pre-rename commit messages **retain "OutboxIQ" verbatim** as accurate historical record (Entry-4 discipline). **If you read "OutboxIQ" anywhere, it is the same product** — either a historical artifact or one of the Entry-30 preserved internal identifiers, which **must NOT be renamed**: extension ID (manifest `key`), storage keys (`outboxiqState`/`outboxiqOnboardingDraft`/`outboxiqAuth`), GCP project ID (`outboxiq-dev` — rename deferred to Premium v1), Client ID, redirect URI. **The GitHub repo was renamed `OutboxIQ` → `fashionably-late` in Session 12** (owner-directed — the Entry-30 "owner's separate call"): the old `github.com/fenil-dedhia/OutboxIQ` URL auto-redirects, the git remote + `PRIVACY_POLICY_URL` (now the `…github.io/fashionably-late/…` Pages path) were updated, and the `OutboxIQState` TypeScript type / `outboxiq*` storage keys / `outboxiq-dev` GCP project stay frozen (internal identifiers, not the repo name). The npm package was renamed `outboxiq-extension` → `fashionably-late-extension`.

## Repository status

> **Tiered product (2026-05-17, owner-directed — Entry 32).** Fashionably Late ships as **two tiers of the same generation**: **Free v1** — extension-only, no backend, free, the **immediate dev + public-launch target** — and **Premium v1** — extension + backend, paid, a **separate later track** built after Free v1 validates demand. Free v1 delivers the core value (recipient-aware Schedule Send incl. Optimize-for-recipient) entirely on-device; the only PRD feature needing a backend (Unschedule-on-Reply) tier-gates to Premium v1. **All current and upcoming feature work targets Free v1.** Premium v1 (the `backend/` + Option-B OAuth + Unschedule-on-Reply) is **not the next work** — it has its own PRD section (**§13**) and checklist (**`PREMIUM_LAUNCH_CHECKLIST.md`**). **"Premium v1" is a paid tier, not "v2"** (v2 = a later generation / post-launch additive direction — see `PRE_LAUNCH_CHECKLIST.md` "v1 vs. v2 decisions"). This is the lens for everything below: where the text says "backend", "refresh token", "Option B", or "Unschedule-on-Reply", read **Premium v1**.

**Current state (Session 13 close; per-session history in `notes/session-*-summary.md`):**

- **Toolchain + build pipeline** (TypeScript + React + Vite + `@crxjs/vite-plugin`, MV3) set up and verified — `npm run build` produces a loadable unpacked extension in `extension/dist/`. The MV3 manifest is generated from a typed `extension/manifest.config.ts`; **permissions: `storage` + `scripting`; host_permissions: `https://mail.google.com/*` only** (Free v1 has no OAuth and no Google API host — Entry 39). The `scripting` permission is the post-Session-10 install-UX fix: the SW programmatically injects the content script into already-open Gmail tabs on `onInstalled`, since MV3's static `content_scripts` only fires on subsequent page loads — without it, a user installing while Gmail is open sees no behaviour until they manually refresh.
- **Storage:** a typed `chrome.storage.local` state layer (PRD §7.2 schema) in `src/lib/`, `schemaVersion`-tagged (see gotchas).
- **PRD §5.1 onboarding — implemented.** 3-step flow (welcome+transparency+consent → set-up-your-timezones → working hours); consolidates the PRD's original 5 steps (PRD §5.1.3 updated to match, Entry 6). Launch is owned by the service worker (lifecycle events + content-script trigger + toolbar icon). Step 2 captures the user's own tz *and* **Pinned Timezones** (up to 5, defaults PST/EST/GMT/CET/IST; PRD §5.1.3 Session-11 amendment); Step 3 uses the Entry-40 "Default boundaries" framing. An already-open Gmail tab installs the §5.2/§5.3/§5.5.1 integrations the moment onboarding finishes — `content-script.ts` subscribes to `chrome.storage.onChanged` (no Gmail refresh needed).
- **PRD §5.2 / §5.3 — fully implemented.** The native "Schedule send" item is relabelled and its click intercepted to open Fashionably Late's enhanced modal (Shadow DOM): §5.3.2 header/subtitle, §5.3.3 Quick Options, the §5.3.3-amendment "Last scheduled time" row, the §5.3.4 custom date/time picker, **and §5.3.5 Optimize-for-X** (items (a)–(n)). The recipient dropdown reads compose To+CC chips (BCC excluded; display names from the DOM; the (To)/(CC) suffix was dropped per owner UX call, §5.3.5 (c); selectors live in `compose-recipients.ts` — see gotchas); cache→inline manual tz picker via the shared searchable `TimezonePicker` (spec (k), with a Pinned section); two timings (9 AM / 1 PM in recipient tz); the timing panel **gated on a selected recipient** (who→when, §5.3.5 (m)); indefinite manual cache TTL (spec (j); `recipient-cache.ts:isCacheEntryFresh` is source-aware). All paths produce real native scheduled sends via the verified Gmail DOM recipe. **Live-Gmail VERIFIED**, including the §5.3.5 Optimize → real scheduled-send chain (driven end-to-end at the Session-12 close-out, which fixed the Gmail multi-dialog race — see gotchas). **Still un-driven-live:** the cache-hit-on-reuse round-trip (unit-tested).
- **PRD §5.5 working-hours/absolute-limits enforcement — implemented**, both triggers. A pure decision module (`src/lib/schedule/working-hours.ts`) computes **both** rule types unconditionally and feeds the §5.3.6 seam; the locked 3-option soft-warning modal (`WorkingHoursWarning.tsx`) fires per the locked split (Schedule Send → absolute only; §5.5.1 regular Send → working hours + absolute). The §5.5.1 regular-Send guard (`src/content/regular-send/`) is a capture-phase, compose-scoped, **whole-gesture** interceptor deciding synchronously off a cached snapshot (`config-cache.ts`), failing toward sending on every ambiguous path (30s watchdog). A pure time-aware `ensureFutureSnap()` (Entry 28) is applied by both the §5.5.1 and §5.3 consumers. See gotchas + Locked product decisions for the binding details.
- **Robustness:** a React error boundary around the §5.3 modal; a **multi-compose safety net** (≥2 composes → native handoff on the real compose-scoped menuItem + relabel revert; the §5.5.1 guard inherits this and deliberately doesn't intercept with ≥2 composes). The full compose-scoping fix is a **v2 deferral, no longer launch-blocking** (Entry 27, argued in `PRE_LAUNCH_CHECKLIST.md` "v1 vs. v2 decisions").
- **Settings (§5.8) — built.** All seven §5.8.2 sections ship (`src/pages/settings/`, sidebar nav), reached via the three §5.8.1 access points (toolbar icon → Settings once onboarded; onboarding completion link; a self-styled gear in the Schedule Send modal — all via `openOptionsPage()`, no new permission). Editable post-onboarding: Pinned Timezones (drag-and-drop + keyboard reorder; order authoritative + live-syncs into an open modal), the manual recipient-tz cache (view/edit/delete/clear), Working Hours + Default boundaries (validity-gated autosave). The two Free-v1 feature toggles are wired to their consumers (`recipientOptimization` → §5.3.5 Optimize-section visibility; `autoRescheduleOnOutsideHours` → §5.5.1 guard early-return, regular Send only). **Privacy & Data is fully wired (Session 13, §6.1.1):** Export My Data downloads the full local state as on-device JSON (anti-omission; minus the 3 non-Free-v1 toggles per Entry 50 — `unscheduleOnReply`/`scheduleConfirmationToast`/`alwaysScheduleOutsideHours` — filtered at the export layer, schema untouched); Delete My Data is a typed-"delete" confirmation → irreversible wipe of the owned `outboxiq*` keys → terminal "deleted" screen / re-onboard (local-only copy, no backend/revoke language — §6.1.2); the Privacy/ToS links are **live** at `https://fashionablylate.app/legal/{privacy,terms}` (custom apex domain — no hyphen — GitHub Pages from `docs/legal/`; `PRIVACY_POLICY_URL`/`TERMS_OF_SERVICE_URL` in `constants.ts`; docs still carry `[DATE TBD]` placeholders to fill pre-launch). About shows v**1.0.0** + a "Built by" LinkedIn row + a `mailto:` support link (feedback channel decided). Data export/delete are **local-only** (no network/telemetry — Entry 39/§11).
- **Free v1 = zero OAuth, zero Google API (Entry 39).** Recipient-timezone resolution is **cache → manual** (`src/background/timezone-cascade.ts`, `{source:"cache"|"manual_needed"}`); recipient identity is DOM-read (§5.3.5). The Sessions 7–9 client OAuth/People/`login_hint` stack was built+verified-live and is **preserved inert** in `src/premium-v1/` (Premium v1). §5.9 Undo + §5.7 badge + Calendar all out of product scope (Entries 35/37). Detail: the **Google Cloud / OAuth** section.
- **`backend/` — untouched; now Premium v1 scope** (not Free v1). Its sole purpose + the Maps-proxy removal carry forward to Premium v1 intact. Free v1 ships with **no backend at all**.

- **Next (Session 14+) — Free v1 is FEATURE-COMPLETE; remaining work is launch hardening, not building (owner roadmap, Entry 52):** **Session 14 = accessibility pass** (WCAG AA, PRD §6.3 — onboarding step-focus/aria-live, modal focus traps incl. the Session-13 delete-confirmation modal, Settings keyboard-nav + AA contrast, full screen-reader walkthrough); then **Google Workspace compatibility** verification; then a **security audit** (no security holes); then **comprehensive hands-on testing of all scenarios**. In parallel (owner-driven, no session): real brand/icons + Web Store listing & submission, fill the legal `[DATE TBD]` placeholders, license-model decision. Optional/exploratory (NOT launch-blocking): the two Gmail-API probes. **No OAuth/CASA/consent-verification work remains for Free v1.** **Premium v1 is OUT of this repo/project (Entry 52):** no Premium work is planned here; the preserved `src/premium-v1/` + `backend/` + `PREMIUM_LAUNCH_CHECKLIST.md` + PRD §13 remain in place **pending an explicit owner decision to physically remove them** — flagged, not executed (they are built/verified work; deleting is hard to reverse). See `notes/session-13-summary.md` §h.

Session 11 built the timezone-picker overhaul + Pinned Timezones (curated dataset, searchable combobox, `pinnedTimezones` §7.2 field at SCHEMA_VERSION 3); Session 12 built the §5.8 Settings panel and renamed the repo `OutboxIQ` → `fashionably-late` — per-session detail (test counts, commit hashes, hands-on iterations) lives in `notes/session-11-summary.md` and `notes/session-12-summary.md`.

## Commands

All commands run from `extension/` (the only component with tooling so far; `backend/` has none yet). Example: `npm --prefix extension run build`.

- `npm install` — install dependencies (first-time setup).
- `npm run dev` — Vite dev server with CRXJS hot-reload, for live development against Gmail. (See the `dist/`-corruption gotcha — prefer one-shot builds for hands-on verification.)
- `npm run build` — typecheck (`tsc --noEmit`) then production build to `extension/dist/`. The DEV-only `__oqAuth` OAuth smoke harness is **stripped** from this build; this is the ship build.
- `npm run build:smoke` / `npm run smoke:check` — **now Premium-v1 OAuth tooling (Entry 39): inert for Free v1.** They build/assert the `__oqAuth` harness in `src/premium-v1/oauth.ts`; since no Free v1 entry imports it, `smoke:check` will report `❌ BUILD BAD` for a Free v1 build — that is **expected, not a regression**. Kept for Premium wire-up (`research/oauth-smoke.md`, `src/premium-v1/README.md`).
- `npm run typecheck` — type-check only, no build.
- `npm run lint` — ESLint (typescript-eslint + react-hooks + react-refresh; Prettier-compatible).
- `npm run format` / `npm run format:check` — Prettier (default config) write / check.
- `npm run test` — Vitest (jsdom + React Testing Library), single run.
- `npm run icons` — regenerate the throwaway placeholder dev icons (real brand work is in `PRE_LAUNCH_CHECKLIST.md`).

**Load the unpacked extension:** Chrome → `chrome://extensions` → enable Developer mode → "Load unpacked" → select `extension/dist/`.

**Non-obvious gotchas (encoded here so future sessions don't rediscover them):**
- **`npm run dev` corrupted `dist/` (Session 8).** A `npm run dev` run left `extension/dist/` containing `node_modules`, the `extension-key.pem`, the full source tree, and a recursively-nested `dist/dist/`, and the service worker would not open from `chrome://extensions`. A clean one-shot `npm run build` / `build:smoke` (with `publicDir:false`) does **not** do this — root cause in CRXJS dev mode not chased (the hands-on smoke path was moved off the dev server to `build:smoke` instead — Entry-25 "design for the real constraint"). Recovery: `rm -rf extension/dist` then a one-shot build; in Chrome **Remove** the old card and **Load unpacked** the fresh `dist/`. Prefer one-shot builds for hands-on verification; treat `npm run dev` as suspect until its dist output is re-validated.
- **Free v1 must NOT import anything from `src/premium-v1/` (Entry 39).** The service worker deliberately does **not** `import "./oauth"` — Free v1 has no OAuth. The OAuth/People stack lives inert in `src/premium-v1/`; wiring it into any Free v1 entry (`service-worker.ts`, `content-script.ts`, pages) re-introduces the install-time `identity`/host permissions and the CASA/consent-verification gate the Entry-39 pivot removed. Audit before shipping: `grep -rn 'from "[^"]*premium-v1' src --include=*.ts | grep -v src/premium-v1/` must be empty. (Premium v1 wires it up later — `src/premium-v1/README.md`.)
- `npm audit` flags 2 high-severity advisories in **dev-only** transitive deps (`rollup` via `@crxjs/vite-plugin`). `npm audit --omit=dev` is clean — nothing vulnerable ships in the extension. Do **not** run `npm audit fix --force` (it breaks the build). This is the accepted CRXJS community-plugin tax (a locked, eyes-open trade-off — see Locked tech decisions).
- The build prints a benign CRXJS-on-Vite-8 warning ("Both `rollupOptions` and `rolldownOptions` … will be ignored"). Content scripts still build and wire up correctly; it is cosmetic and internal to CRXJS.
- `vitest.config.ts` is intentionally excluded from `tsconfig.json`'s `include` (a Vite 8 ↔ Vitest-nested-Vite types skew, not a code defect). Test code under `src/` is still fully type-checked.
- **MV3 entry files must have distinct basenames.** Under Vite 8/Rolldown, CRXJS collapses two entries that share a basename (e.g. `src/background/index.ts` + `src/content/index.ts`) into one chunk, and the service worker silently ends up running the content script's code (no error; onboarding just never opens). The fix already applied: the entries are `src/background/service-worker.ts` and `src/content/content-script.ts`. Do not rename them back to `index.ts`. To sanity-check after a build: `dist/service-worker-loader.js` must import the `service-worker.ts-*` chunk, not the `content-script.ts-*` one.
- **Local storage carries a `schemaVersion`.** The `outboxiqState` root (PRD §7.2) has a `schemaVersion` field (currently `3`, the `SCHEMA_VERSION` constant in `src/lib/constants.ts`). There is intentionally **no migration framework yet** — only the field. So far every bump has been *purely additive*, so `getState()`'s default-merge IS the migration (no version branch needed yet): v1→v2 added `lastScheduled`, v2→v3 added `pinnedTimezones` (Session 11, defaults to `[]`). When a future change alters the §7.2 shape *non-additively*, bump `SCHEMA_VERSION` and branch on the stored value in `getState()`.
- **Gmail DOM knowledge is isolated to two modules, each with a probe.** (1) `src/lib/schedule/gmail-recipe.ts` — the *single point of failure* for Gmail Schedule-Send automation (every selector + the synthetic-event recipe). Canonical refs: `research/scheduled-send-api-spike.md` (preset path), `research/pick-date-time-probe.md` (custom path + relabel target), `research/send-button-probe.md` (§5.5.1 Send button + whole-gesture/capture/replay). (2) `src/content/compose/compose-recipients.ts` — the single point of failure for **§5.3.5 compose-recipient reading**; anchor `div[role="option"][data-hovercard-id]` scoped to the chevron's compose pane (canonical ref: `research/compose-recipients-probe.{js,md}`, live-verified 2026-05-20 — the originally-shipped `input[name="to"|"cc"]`/`[email]` guess was wrong). Run the matching probe to rediscover selectors when Gmail breaks: Schedule Send → spike/pick-date-time probes; regular-Send guard → `send-button-probe.js`; recipient reading → `compose-recipients-probe.js` (`__flComposeProbe.discover()`). When any breaks post-launch, fix it in that one module.
- **Gmail's compose window is ITSELF a `[role="dialog"]`, and the Schedule-Send picker mounts as a *separate, fresh* dialog (found live, Session-12 close-out 2026-05-22).** So `document.querySelectorAll('[role="dialog"]')` returns **≥2** nodes during scheduling, and the schedule menu/picker rows render a tick *after* their dialog node appears. Two rules `schedule-actions.ts` must respect (and now does): (1) **don't** "resolve the schedule dialog then query it once" — you can get the compose dialog, or query before rows render; instead `waitFor` the actual target element **document-wide** (the `.Az.AM` "Pick date & time" row; the matching preset row). (2) **don't** pin a dialog node reference across the menu→picker swap — Gmail tears the menu node down, so the reference goes stale; re-find the picker **fresh by its contents** (`findPickerFields`: the dialog holding a date-shaped + a time-shaped `<input>` value). Violating either caused two intermittent live failures ("Pick date & time row not found"; "custom date/time inputs" timeout). Ref: `research/pick-date-time-probe.md` 2026-05-22.
- **§5.5.1 intercepts the WHOLE Send gesture, not one event.** The probe proved Gmail finalises the send on a *later* event of the gesture (pointerup/click), so the regular-Send guard captures pointerdown+mousedown+pointerup+mouseup+click **and** ⌘/Ctrl+Enter — a single-event hook leaks (the one-shot `armSuppress` "sent anyway" that the gate caught). It only ever `preventDefault`s on a *certain* violation (decided synchronously off the cached config); everything else falls through to native Send. Do not "simplify" it to a click-only listener.
- **Duplicate content-script instances → use PAGE OWNERSHIP, not a `window` latch (Session 13, bug reported 3×).** Reloading/updating the extension while a Gmail tab stays open leaves the OLD instance ORPHANED (listeners alive, `chrome.*` context severed) alongside a fresh one. Two instances fight: two §5.5.1 guards cancel each other's Send replay ("Send now anyway does nothing"); the orphaned §5.2 interceptor wins a Schedule-Send click but can't read storage → falls back to Gmail's NATIVE modal; relabel/chevron churn. A latch on `window` does NOT fix it — a reloaded instance runs in a SEPARATE isolated world with its own `window`. The fix (`src/content/page-install-latch.ts`): a last-writer-wins ownership **token in a `<html>` attribute** (the DOM is shared across all worlds) + a `contextAlive()` (`chrome.runtime.id`) check; `claimPageOwnership()` on install, and `isCurrentOwner()` guards the top of the §5.2 interceptor, the relabel observer, AND the §5.5.1 send guard so only the newest LIVE copy acts. Do NOT revert to a `window`/module latch, and do not have any of those three handlers act without the `isCurrentOwner()` check. This is single-world-impossible to unit-test — verify hands-on (reload ext with Gmail open, no refresh → Schedule Send opens OUR modal, Send-now-anyway sends). See owner-decisions-log Entry 51.
- **React inside Shadow DOM (the §5.3 modal).** Quirks, encoded so they aren't rediscovered: (1) `createRoot` must target an element **inside** the shadow root, not the host — since React 17 event delegation roots at the `createRoot` container, so rooting it in the shadow tree is what makes synthetic events fire for shadow-rendered controls; (2) page CSS does not cross the shadow boundary, so the modal's styles **must** be injected into the shadow root (`schedule-modal/styles.ts`) — the upside is hard isolation in both directions (Gmail can't break us, we can't leak into Gmail); (3) keep one host at a time — re-opening removes the previous host so a stale React root can't linger. See `src/content/schedule-modal/mount.tsx`.
- **The content script now bundles React** (~190 kB `jsx-runtime` chunk) because the §5.3 modal is React and is imported by `content-script.ts`. It loads on every Gmail page. Acceptable for v1/MVP; a later optimisation is to lazy-load the modal bundle only when the user actually triggers Schedule Send. Flagged, not yet done.

## Source-of-truth documents

Read these before making non-trivial decisions — they encode constraints that aren't visible in the code:

- `Fashionably_Late_PRD.md` — full spec. **§11 "Out of Scope: Do Not Build"** is unusually load-bearing: it explicitly forbids 20 plausible-sounding features (email tracking, AI rewriting, analytics dashboards, separate scheduled-emails view, multi-account, holiday awareness, Firefox support, telemetry, etc.). Treat it as a hard constraint, not a suggestion — do not infer or add features outside it. **§13 "Premium v1 Scope"** is the opposite of §11: it holds the *preserved, intact* design for the backend-dependent features (Unschedule-on-Reply, the backend service, Premium Option-B OAuth) that are **tier-gated to Premium v1, deferred from Free v1, NOT deleted**. Don't conflate §11 (never build) with §13 (build, in the Premium tier). The §1 amendment is the tier-split overview.
- `PRE_LAUNCH_CHECKLIST.md` — **Free v1** public-launch obligations (Free-v1 OAuth Production + any required restricted/sensitive-scope assessment, Privacy/Terms Free-v1 drafting, brand work, Web Store submission, pre-launch probes). `PREMIUM_LAUNCH_CHECKLIST.md` — **Premium v1** launch gates (CASA Tier 2, backend infra, online→offline OAuth switch, backend Privacy/ToS, Free→Premium migration, billing). Anything you'd otherwise spend time on for "launch readiness" belongs in one of these, not in feature code — pick the tier.
- `notes/session-*-summary.md` — per-session history; `session-12-summary.md` is the latest (Session 12 close + the 2026-05-22 post-close-out addendum), with `session-1`…`session-11` preceding it.
- `notes/owner-decisions-log.md` — running record of moments where owner/PM input materially changed the build's trajectory, with honest counterfactuals (48 entries through Session 12). Portfolio + coaching artifact; **must be maintained every session** (see "Working in this repo"). This is the authoritative *why* — CLAUDE.md references it rather than restating it.

## Architecture

Two independently-deployed components, kept deliberately small:

### `extension/` — Chrome MV3 (does almost everything)

Local-first by design. All recipient-timezone detection, scheduling UI, working-hours logic, and storage runs in the browser. The extension **enhances** Gmail's native Schedule Send rather than replacing it — the native dropdown is rebranded and the native modal is enriched, but Gmail's interaction patterns and the Scheduled label are preserved (PRD §8.1 "native feel over branded feel"). Layout: `src/background/` (Free v1 service worker — **no OAuth**; cache→manual tz resolver), `src/content/` (Gmail DOM injection), `src/pages/{onboarding,settings}/`, `src/lib/` (shared utilities), `src/premium-v1/` (**inert, preserved Premium OAuth/People stack — not imported by Free v1**; see its README), `public/icons/`. **Free v1 makes no Google API calls (Entry 39).**

### `backend/` — EU-hosted relay (Premium v1 only; exists for exactly one reason)

> **Premium v1 scope (Entry 32).** Not part of Free v1 — Free v1 ships extension-only. This block describes the **Premium v1** backend (PRD §13), gated by `PREMIUM_LAUNCH_CHECKLIST.md`. It is **not the next work**.

The backend is **not** a general server. It exists for one server-side concern and nothing else: the **Unschedule-on-Reply relay** — subscribes to Gmail push notifications via Google Cloud Pub/Sub and cancels scheduled emails when a reply arrives (PRD §5.6, §7.3).

- **Google Maps was removed from product scope (Entry 26 — removed, not deferred).** The former Maps Geocoding/Time Zone proxy is gone entirely: no Maps API key, billing, OAuth scope, or proxy endpoint, ever. Recipient timezone is fully on-device (cache → People → Workspace Directory → manual). Do not reintroduce without re-opening Entry 26. Rationale: PRD §5.4.1 amendment, §7.3.1.
- **OAuth token management is part of the one purpose, not a second one (Entry 31; PRD §7.5/§7.3.1).** The backend's purpose is Unschedule-on-Reply *and the OAuth token management that enables it*: §5.6 needs backend-initiated Gmail calls when the extension isn't running ⇒ a backend refresh token ⇒ the backend runs the auth-code exchange ("Option B"), necessarily reused by the extension's other Google calls (one grant per user). Not a second purpose; not a reversal of Entry 26.

Do not add other endpoints, telemetry sinks, user-data storage, or "while we're here" features. If you find yourself wanting to, that's a signal to push the logic back into the extension or to stop.

**Data rules:** no email content is ever stored. Stored data is limited to user email, encrypted OAuth refresh token (per-user keys), and active scheduled-message records. EU region (Frankfurt/Amsterdam) for GDPR data-residency.

## Locked tech decisions

Already decided — do not re-litigate without explicit user input:

- **Extension:** React + Vite, **TypeScript**, Manifest V3. Build tooling is **Vite + `@crxjs/vite-plugin`**, chosen for content-script hot-reload during development; we explicitly accept that CRXJS is a community plugin that may occasionally lag Vite releases. Locked 2026-05-15. (Rationale for TypeScript: the owner is non-technical and relies on Claude Code for implementation, so compile-time error-catching and explicit architecture matter more here than for a team that debugs runtime issues fluently — Entry 5.)
- **Backend + Database (Premium v1 only):** Node + Hono on Fly.io, EU region; Supabase, EU region. **These are *Premium v1* tech locks, not Free v1** (Free v1 has no backend; Entry 32). Locked — do not re-litigate for the eventual Premium build, and do not read their presence as Free v1 needing a backend.
- **License:** All-rights-reserved / proprietary during v1 development (changed from MIT on 2026-05-15 when the repo went public for portfolio visibility — public to read, no reuse rights granted; Entry 4). The eventual public-launch commercial model remains a pre-launch decision. Do not "restore" MIT — the all-rights-reserved choice is deliberate.
- **Telemetry:** zero. No analytics SDKs, no usage events, no opt-in hedge. Success metrics are qualitative for v1.
- **Privacy/ToS docs:** drafting deferred; will live in `docs/` and be served from GitHub Pages.
- **Brand assets:** placeholder "OQ" mark during dev; real brand work happens just before Web Store submission.
- **Brand-independent stable identifiers (locked 2026-05-17; Entry 30).** The product brand name may change before launch; these technical identities are deliberately decoupled from it and **must NOT be renamed on a rebrand** (renaming buys nothing and breaks identity/data/tokens): the **extension ID** (from the manifest `key`, not `name` — `dicnmcmhapcfceodecocnkaacjdpplnm`), the **OAuth Client ID** + `chromiumapp.org` **redirect URI**, the **GCP project ID** (`outboxiq-dev`) and any future Fly/Supabase resource names, and the **storage keys** (`outboxiqState`/`outboxiqOnboardingDraft` in `src/lib/constants.ts`). A rebrand = display copy + assets + a deliberate PRD/locked-copy amendment (the verbatim `"…powered by Fashionably Late"` label + onboarding privacy copy are spec text) — nothing more. Rename-proofing the brand-named Privacy/ToS/Pages URLs is a tracked launch-blocker (`PRE_LAUNCH_CHECKLIST.md` "Naming / rebrand readiness"). A `PRODUCT_NAME`-constant centralisation of incidental copy is an explicitly-deferred optional future task — do not treat its absence as drift.

## Locked product decisions

Product/UX decisions locked by the owner — do not re-litigate without explicit user input. (Reasoning + counterfactuals in the cited entries.)

- **§5.5 enforcement is a soft warning, not a hard block and not a silent auto-snap (locked 2026-05-16; Entries 19–20).** On a working-hours or absolute-limits violation, show a soft-warning modal that names the violation in plain language (e.g. "You set 7:00 AM as your earliest send time. This email is scheduled for 3:33 AM. What would you like to do?") and offers exactly three explicit choices: **(1) Proceed anyway** (honour the user's time, overriding just this once); **(2) Snap to [the violated boundary]**; **(3) Cancel** (close, leave the user in compose to adjust). A hard block is too paternalistic (legitimate overrides exist) and a silent auto-snap violates §8.3 "sensible defaults, easy overrides" — a soft warning gives agency. Same pattern for both rule types; **if both are violated at once, surface the absolute-limit violation** (the harder constraint). Do not replace this with a block, an auto-snap, or a 2-option variant.
- **Warnings fire for unintended actions, not the product's core use case (locked 2026-05-16; Entry 21).** The §5.5 trigger is **split**: **Schedule Send warning = absolute limits only**; **Regular Send (§5.5.1) warning = working hours + absolute limits**. Deliberately *scheduling* outside one's working hours to land in a recipient's window is exactly what Fashionably Late is *for* — warning on it trains users to dismiss the modal. An *immediate* off-hours Send is plausibly unintended, so §5.5.1 warns on both. The calc (`working-hours.ts`) still computes **both** rule types unconditionally — **do not delete the working-hours branch**; it is consumed by §5.5.1 and §5.3.5 (advisory input). The **default 9 AM working-hours start is retained**: under this split it does not nag Schedule Send but does drive the §5.5.1 regular-Send prompt — signal, not noise (a user whose pattern differs adjusts onboarding). Do not re-litigate the default or the split.
- **§5.5 snap-target resolution (locked 2026-05-16; Entry 19).** "Snap to [boundary]" resolves per rule type. **Working-hours violation** → the §5.5.3 next-working-day algorithm (next configured working day at that day's working-hours start). **Absolute-limit violation** → the absolute boundary (`absoluteEarliest`/`absoluteLatest`) **on the same calendar day the user originally picked** — *not* next-working-day (absolute limits are clock-time rules, not day-of-week rules; the user chose their target day deliberately). Consistent with PRD §5.1.3's "Earliest I'd ever send an email" framing.
- **§5.5 absolute snap may land on a non-working day — intentionally permitted in v1 (locked 2026-05-16).** Because an absolute snap keeps the user's chosen day, it can land on a non-working day (e.g. Sat 7:00 AM with Mon–Fri hours). **Deliberate**: the explicit day choice overrides the working-hours *soft* preference. Do **not** "fix" this in v1 by re-snapping or chaining a second warning. A v2 secondary informational note is **deferred, not v1 scope; do not implement.**
- **§5.5 past-`after-latest`-snap forward-roll (locked 2026-05-17; Entry 28; refines — does not overturn — the two bullets above).** "Absolute snap = the violated boundary on the same calendar day" is correct only when that day is a *future day the user picked*. For an **`after-latest`** violation the same-day ceiling is **in the past** when there is no picked future day (the §5.5.1 regular-Send trigger — requested time *is* "now"; or a later-today Schedule-Send pick already past the ceiling); Gmail rejects a past time. Locked resolution: an `after-latest` snap not safely in the future **rolls forward to the next working morning** (soonest configured working day strictly after now, at its start; **falls back to next calendar day at `absoluteEarliest`** if zero working days). Scoped to `after-latest` **only** — every other snap is provably already strictly future, so the behaviour above (incl. the intentional non-working-day landing of a *future* picked absolute day) is untouched while still future. Implemented as a pure, time-aware `ensureFutureSnap()` applied by both the §5.5.1 and §5.3 consumers; `checkWorkingHours` stays unchanged/`Date.now`-free. Do not re-collapse this into "always same-day" or delete the forward-roll.
- **§5.5.1 regular-Send-button trigger — IMPLEMENTED (Session 6; clears the Session-5 deferral).** PRD §5.5.1 first bullet is built (probe-gated against live Gmail before any code — Entry 23). It guards the primary Send button + ⌘/Ctrl+Enter; the §5.2.3 "never block native Gmail" risk is contained by failing toward sending on every ambiguous path. PRD §5.5.1 amended (deferred → implemented).
- **§5.5.2 auto-suppress footer checkbox — dropped for v1 (Session 5 owner decision).** The locked soft-warning pattern requires an explicit choice every time, so PRD §5.5.2's "Always schedule for next working day when outside hours" suppression checkbox is not built (PRD §5.5.2 amended). Consequence (not a bug to "fix"): the `alwaysScheduleOutsideHours` §7.2 field and its planned §5.8 Settings toggle are **inert in v1** by design — left in place for schema stability but unused. Do not wire them to silent auto-snap behaviour.
- **"Default boundaries" rename + Optimize-for-X exception (locked 2026-05-19; Entry 40; refines, does NOT invalidate, Entries 19/20/21/28 above — "absolute-limit violation" in those reads as "Default-boundaries violation" going forward).** "Absolute limits"/"hard limits" are renamed **Default boundaries** product-wide (user-facing copy: §5.1.3, §5.5, §5.8.2 — internal schema fields `absoluteEarliest`/`absoluteLatest` deliberately KEPT as stable identifiers per Entry-30, no SCHEMA_VERSION bump). The §5.5 soft-warning trigger **narrows**: still fires on Quick Options / Pick Custom / "Last scheduled time" / §5.5.1 regular Send violations (**Case 2 — manual**); does **NOT** fire when the time was computed by §5.3.5 Optimize-for-X (**Case 1 — algorithmic/feature-mediated**) — the four-step engagement is explicit intent, exempting it extends Entry-21's "warnings fire for unintended actions, not the core use case" to Optimize-for-X. Three-choice soft-warning pattern, snap-targets, `ensureFutureSnap`, §5.5.1 split — **all preserved unchanged.** PRD §5.5 + §5.3.5 carry the binding text.
- **Curated timezone dataset + Pinned Timezones (locked 2026-05-20; Entries 44–45).** The shared `TimezonePicker` renders a **curated, offset-labelled dataset** (`src/lib/timezone/curated-timezones.ts`) — do **not** revert to enumerating raw `Intl.supportedValuesOf` IANA ids. Stored values stay canonical IANA ids (display resolves legacy/browser ids to their group, never silently migrates). **Label grammar (do not violate):** zone-name + grouped cities | single country + its cities | bare country list — never a country name mixed with another country's cities (India/Sri Lanka are separate rows). No "(no DST)" noise (DST = abbr pair or "(DST)"). ALL-CAPS search = case-sensitive abbreviations. **Pinned Timezones**: cap **5**, defaults PST/EST/GMT/CET/IST, **no silent pinning** of existing users (state defaults to `[]`; only the onboarding draft pre-checks). PRD §5.1.3 / §5.3.5 (k) / §5.8.2 carry the binding text.
- **§5.3.5 Optimize-for-X UX — locked, ready for Session 10 build (locked 2026-05-19; Entry 40).** Items (a)–(n) in PRD §5.3.5 are the authoritative spec: Optimize section visible+unchecked-on-open; recipient dropdown from compose DOM **To+CC** (BCC excluded), each entry labelled `(To)`/`(CC)`; display names from compose DOM (never-emailed → email shown; no People/Workspace lookup — Free v1 is OAuth-free); two timing options only — Morning 9 AM / Midday 1 PM (the "End of day 4 PM" option **dropped from product scope**, not deferred); inline timezone picker on cache-miss with **no default pre-selection** (NOT user's own tz) + "Remember…" checkbox default-checked; manual cache **TTL indefinite** (was 90d when API-detected, now user-entered data); the IANA tz picker **MUST be the same component** as onboarding §5.1.3 Step 2 (binding architectural constraint, to prevent drift); no "I don't know" fallback (correct behaviour is to uncheck Optimize). Tooltip framed as **general research**, not Fashionably Late tracking (§11 binding).

## Gating constraint: Gmail scheduled-send spike — CLEARED (2026-05-15)

The Gmail API does not expose third-party scheduled sends, so a technical spike gated all Schedule Send / Pub/Sub / recipient-cache work. **Cleared & verified** (`research/scheduled-send-api-spike.md`): Approach C (UI-automate Gmail's own Schedule Send) confirmed end-to-end, and the API cancel path (`messages.list?q=in:scheduled` → `messages.trash`) verified. The custom "Pick date & time" path was subsequently verified too (`research/pick-date-time-probe.md`). Still open (not blocking; tracked in `PRE_LAUNCH_CHECKLIST.md`): Workspace compatibility before public launch.

## Google Cloud / OAuth

> **Free v1 has NO OAuth and NO Google API (owner-decisions-log Entry 39, 2026-05-19).** Zero scopes, no `chrome.identity`, no token, no consent screen — recipient timezone is cache → manual, recipient identity is DOM-read (PRD §5.3.5/§5.4.1 Entry-39 amendments). **Consequence: no CASA, no sensitive-scope consent-screen verification gate** — a major pre-launch simplification. Nothing in this section applies to Free v1's active path.

**The full OAuth/People stack is BUILT, hands-on-verified (Sessions 7–9), and PRESERVED INERT for Premium v1** in `extension/src/premium-v1/` — see that directory's `README.md` for contents + wire-up, and PRD §7.5/§13/§13.3 for the design. It is not deleted, not commented-out: compilable, unit-tested, and imported by **no** Free v1 entry point (audit: `grep -rn 'from "[^"]*premium-v1' src --include=*.ts | grep -v src/premium-v1/` → empty). Highlights preserved there: `launchWebAuthFlow` Web-app client (Entry 29; multi-account chooser), implicit grant + OpenID `id_token` `login_hint` (Entry-34→38 multi-account silent renewal, **verified live** Session 9), People `searchContacts`, the full API cascade, scopes `contacts.readonly`+`userinfo.email`+`openid`. Premium adds backend Option-B refresh tokens (Entry 31; PRD §13.3). `npm run build:smoke`/`smoke:check` + `research/oauth-smoke.md` are now Premium-scoped OAuth tooling (inert — no entry imports the harness).

GCP project **`outboxiq-dev`** + the stable extension ID (`dicnmcmhapcfceodecocnkaacjdpplnm`, manifest `key`) remain locked, brand-independent identifiers (see "Locked tech decisions" / Entry 30) — unchanged by the Free v1 simplification; the GCP consent screen is simply unused by Free v1.

The Free v1 roadmap (what's done, next, and the Premium-v1 follow-on) is the **Current state** section above; per-session detail is in `notes/session-*-summary.md`.

## Working in this repo

- The user (Fenil) is non-technical for implementation details. Narrate decisions in plain English, and confirm before destructive or hard-to-reverse actions.
- When a feature touches privacy, OAuth scopes, data residency, or anything the PRD §11 list could conceivably bear on, re-read the relevant PRD section before writing code rather than working from memory.
- If you add tooling (package manager, build, lint, test, deploy), record the resulting commands in this file's "Commands" section so future sessions don't have to rediscover them.
- **Session close-out obligation.** At every session end, alongside updating the session summary, append to `notes/owner-decisions-log.md` either a new entry (using that file's fixed structure) for any moment where owner/PM input materially changed the build's trajectory, or the single line `Session N — no entries this session.` if none qualified. "No entries" is a legitimate and common outcome — do not manufacture entries to fill the log. The honesty rule in that file is binding: counterfactuals must be accurate not flattering, and owner judgment that was wrong or incomplete is logged too.
