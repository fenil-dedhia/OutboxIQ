# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

> **Tiered product (2026-05-17, owner-directed — pre-Session-8, after Entry 31; `notes/owner-decisions-log.md` Entry 32).** OutboxIQ ships as **two tiers of the same generation**, sharing the implemented extension foundation: **Free v1** — **extension-only, no backend, free, the immediate development + public-launch target** — and **Premium v1** — extension + backend, paid, a **separate later track** built after Free v1 validates demand. Free v1 still delivers the core value (recipient-aware Schedule Send incl. Optimize-for-recipient) entirely on-device; the only PRD feature that needs a backend (Unschedule-on-Reply) tier-gates to Premium v1. **All current and upcoming feature work targets Free v1** (Sessions 8+ = extension-side OAuth `access_type=online` + Calendar/People/Directory + recipient cache + §5.3.5 Optimize UI). Premium v1 (the `backend/` + Option-B OAuth + Unschedule-on-Reply) is **not the next work** — it has its own PRD section (**PRD §13**) and its own checklist (**`PREMIUM_LAUNCH_CHECKLIST.md`**). "Premium v1" is a paid tier, **not** "v2" (which in this repo means a later generation / post-launch additive direction — see `PRE_LAUNCH_CHECKLIST.md` "v1 vs. v2 decisions"). This note is the lens for everything below: where the text says "backend", "refresh token", "Option B", or "Unschedule-on-Reply", read **Premium v1**.

Toolchain scaffolded; **PRD §5.1 onboarding is implemented** (first feature). The `extension/` build pipeline (TypeScript + React + Vite + `@crxjs/vite-plugin`, MV3) is set up and verified end-to-end — `npm run build` produces a loadable unpacked extension in `extension/dist/`. Done: a typed `chrome.storage.local` state layer (PRD §7.2 schema) in `src/lib/`, and the onboarding flow as a **3-step** flow (welcome+transparency+consent → timezone → working hours) — note this consolidates the PRD's original 5 steps; PRD §5.1.3 has been updated to match. **PRD §5.2 (compose integration) and §5.3 are implemented except §5.3.5/§5.3.7** (Session 4): the native "Schedule send" item is relabelled and its click intercepted to open OutboxIQ's enhanced modal (Shadow DOM, decision #2). Working: §5.3.2 header/subtitle, §5.3.3 Quick Options, the §5.3.3-amendment **"Last scheduled time"** row (schema v2 `lastScheduled`), and the **§5.3.4 custom date/time picker** — all produce real native scheduled sends by driving Gmail's own UI via the verified recipe. The custom-path DOM was verified by the Session 4 probe (`research/pick-date-time-probe.md` Result log); the `innerTarget` hit-test guard in `gmail-recipe.ts` was the fix that unblocked it. **§5.3.5 Optimize-for-recipient + §5.3.7 fallback** are deferred whole to a dedicated next session (need OAuth + People API; **no Maps** — Google Maps was removed from product scope, see PRD §5.4.1 amendment / Entry 26). Settings (§5.8) is still a placeholder stub. **End-to-end VERIFIED** (2026-05-16): the probe's `live({confirm:true})` scheduled a real email at the exact target time (Gmail's own "Send scheduled for…" toast), in all three compose contexts. **Session 5 added: §5.5 working-hours/absolute-limits enforcement is implemented for the Schedule Send path** — a pure decision module (`src/lib/schedule/working-hours.ts`) feeds the §5.3.6 seam, and a soft-warning modal (`WorkingHoursWarning.tsx`, locked 3-option pattern — see "Locked product decisions") fires on a violation; PRD §5.5.1/§5.5.2/§5.5.3 amended to match. The §5.5.1 *regular-Send-button* trigger is deferred to its own session. Also added: a **React error boundary** around the §5.3 modal (closes the Session 4 §5.2.3 render-throw hole), and a **multi-compose safety net** (≥2 composes → native handoff on the real compose-scoped menuItem, since the global chevron query else mis-targets the leftmost compose — the **full** compose-scoping fix is a **v2 deferral, no longer launch-blocking** (reframed Session 6, owner-directed — argued in `PRE_LAUNCH_CHECKLIST.md` "v1 vs. v2 decisions"; Entry 27)). Session 5 smoke test: 5/6 scenarios green; Scenario 4 (multi-compose) is the mis-target now covered by the safety net. **Session 5.5 added:** the §5.5 Schedule Send warning is **narrowed to absolute-limit violations only** (working-hours violations no longer warn on *scheduling* — that is the core use case; the calc still computes them for §5.5.1/§5.3.5); multi-compose now also **reverts the relabel** to Gmail's native text; and the §5.3.4 custom picker **blocks past dates/times** (native `min` + a fresh-now click-time guard). The **§5.5.1 regular-Send-button trigger is its own next session (Session 5.6)** — probe-gated (the primary Send button DOM is unverified) and the highest-criticality change (a bug = users can't send). PRD §5.5 + §5.3.5 amended; "warnings fire for unintended actions" + default-9 AM rationale locked. **Session 6 added: §5.5.1 the regular-Send-button trigger is now IMPLEMENTED and the probe-gate cleared.** A DOM probe (`research/send-button-probe.{js,md}`) was written, committed, and run hands-on against live Gmail across 4 contexts (new-compose / 2-compose / inline-reply / pop-out) **before** any code (Entry-2/23 spike-gating; the probe's one-shot `armSuppress` produced an ambiguous "sent anyway", sharpened to `armBlockGesture` which proved capture-phase suppression works and replay re-sends — gate cleared, no design change). Implemented: a capture-phase, compose-scoped, **whole-gesture** interceptor (`src/content/regular-send/`) — pointer/mouse on the Send button + ⌘/Ctrl+Enter — that blocks **only on a real violation** decided **synchronously** off a cached working-hours snapshot (`config-cache.ts`; can't await storage mid-gesture), acting on the **FULL verdict** (working-hours OR absolute — the locked split). The locked `WorkingHoursWarning` is reused via a standalone Shadow-DOM mount with a new `context="send"` (present-tense copy + "Send now anyway"; §5.3's callsite unchanged). Every ambiguous/failure path **fails toward sending** (no-config / calc-throw / mount/render-throw → email sends; 30s watchdog so the guard can never wedge Send); **≥2 composes deliberately doesn't intercept** (inherits the §5.2 multi-compose safety-net — a documented v1-acceptable interim, **not** launch-blocking; full fix is a v2 deferral, Entry 27). `gmail-recipe.ts` gained the probe-verified Send anchors + `composeCount()` (now the single source of truth shared with the §5.2 safety net). PRD §5.5.1 amended (deferred → implemented). **Session 7 added:** Phase 1 hands-on smoke (gate zero) of the assembled §5.5.1 guard caught a **default-reachable bug** — an absolute `after-latest` violation snapped to today's ceiling, which for the regular-Send trigger ("now" *is* the requested time, past the ceiling) is always in the **past** → Gmail "Invalid time". Fixed with a pure, time-aware `ensureFutureSnap()` that rolls **only** that one past-prone case to the next working morning (owner decision; fallback next-day-at-earliest); scoped to `after-latest` only (every other snap is provably already future), applied by both §5.5.1 and §5.3 (a narrower latent sibling), `checkWorkingHours` unchanged/Date.now-free; PRD §5.5.3 + "Locked product decisions" amended (refine, not overturn). Also Session 7: **GCP/OAuth foundation is now active** (see "Google Cloud / OAuth" — `outboxiq-dev`, Testing mode, Web-app client, pinned stable extension ID, `oauth-config.ts`); Phase 3 (the OAuth *flow* + §5.4 cascade) deferred to Session 8. 92 tests (was 85); typecheck/lint/Prettier/build/sw-loader green. The `backend/` is untouched and is now **Premium v1 scope** (not Free v1) — not needed until Unschedule-on-Reply, which is itself Premium v1; its **sole** backend purpose and the Maps-proxy removal carry forward to Premium v1 intact. Free v1 ships with **no backend at all**. The MV3 manifest is generated from a typed `extension/manifest.config.ts` (the old 4-field `manifest.json` stub was superseded and removed); permissions remain minimal/just-in-time — `storage` plus `host_permissions` scoped to `https://mail.google.com/*` only (no `tabs`, no OAuth/`identity` yet). Onboarding launch is owned by the service worker via lifecycle events + a content-script trigger + the toolbar icon. See the Commands section below.

## Commands

All commands run from `extension/` (the only component with tooling so far; `backend/` has none yet). Example: `npm --prefix extension run build`.

- `npm install` — install dependencies (first-time setup).
- `npm run dev` — Vite dev server with CRXJS hot-reload, for live development against Gmail.
- `npm run build` — typecheck (`tsc --noEmit`) then production build to `extension/dist/` (the loadable unpacked extension). The DEV-only `__oqAuth` OAuth smoke harness is **stripped** from this build.
- `npm run build:smoke` — same clean one-shot build **plus** the `__oqAuth` OAuth console harness (`OQ_SMOKE=1` → `vite.config.ts` `define` `__OQ_SMOKE__`). For the owner's hands-on OAuth verification (`research/oauth-smoke.md`). Do **not** use this for the shippable artifact — plain `npm run build` is the ship build (harness absent; verified by grepping `dist/assets` for `__oqAuth`).
- `npm run typecheck` — type-check only, no build.
- `npm run lint` — ESLint (typescript-eslint + react-hooks + react-refresh; Prettier-compatible).
- `npm run format` / `npm run format:check` — Prettier (default config) write / check.
- `npm run test` — Vitest (jsdom + React Testing Library), single run.
- `npm run icons` — regenerate the throwaway placeholder dev icons (real brand work is in `PRE_LAUNCH_CHECKLIST.md`).

**Load the unpacked extension:** Chrome → `chrome://extensions` → enable Developer mode → "Load unpacked" → select `extension/dist/`.

**Non-obvious gotchas (encoded here so future sessions don't rediscover them):**
- **`npm run dev` corrupted `dist/` (Session 8).** A `npm run dev` run left `extension/dist/` containing `node_modules`, the `extension-key.pem`, the full source tree, and a recursively-nested `dist/dist/`, and the service worker would not open from `chrome://extensions`. A clean one-shot `npm run build` / `build:smoke` (with `publicDir:false`) does **not** do this — root cause in CRXJS dev mode not chased (the hands-on smoke path was moved off the dev server to `build:smoke` instead — Entry-25 "design for the real constraint"). Recovery: `rm -rf extension/dist` then a one-shot build; in Chrome **Remove** the old card and **Load unpacked** the fresh `dist/`. Prefer one-shot builds for hands-on verification; treat `npm run dev` as suspect until its dist output is re-validated.
- **The service worker must `import "./oauth"`.** OAuth only runs where `chrome.identity` lives (the SW — §6.5). `src/background/oauth.ts` attaches the smoke harness and houses `getAccessToken()` **only when its module is loaded**; nothing else imports it yet, so `service-worker.ts` carries a deliberate side-effect `import "./oauth"`. Do not "tidy" it away as an unused import — removing it silently disables OAuth and the harness (the Session-8 bug that made `__oqAuth` undefined). The Phase-3 message bridge will add a named import alongside it.
- `npm audit` flags 2 high-severity advisories in **dev-only** transitive deps (`rollup` via `@crxjs/vite-plugin`). `npm audit --omit=dev` is clean — nothing vulnerable ships in the extension. Do **not** run `npm audit fix --force` (it breaks the build). This is the accepted CRXJS community-plugin tax (a locked, eyes-open trade-off — see Locked tech decisions).
- The build prints a benign CRXJS-on-Vite-8 warning ("Both `rollupOptions` and `rolldownOptions` … will be ignored"). Content scripts still build and wire up correctly; it is cosmetic and internal to CRXJS.
- `vitest.config.ts` is intentionally excluded from `tsconfig.json`'s `include` (a Vite 8 ↔ Vitest-nested-Vite types skew, not a code defect). Test code under `src/` is still fully type-checked.
- **MV3 entry files must have distinct basenames.** Under Vite 8/Rolldown, CRXJS collapses two entries that share a basename (e.g. `src/background/index.ts` + `src/content/index.ts`) into one chunk, and the service worker silently ends up running the content script's code (no error; onboarding just never opens). The fix already applied: the entries are `src/background/service-worker.ts` and `src/content/content-script.ts`. Do not rename them back to `index.ts`. To sanity-check after a build: `dist/service-worker-loader.js` must import the `service-worker.ts-*` chunk, not the `content-script.ts-*` one.
- **Local storage carries a `schemaVersion`.** The `outboxiqState` root (PRD §7.2) has a `schemaVersion` field (currently `1`, the `SCHEMA_VERSION` constant in `src/lib/constants.ts`). There is intentionally **no migration framework yet** — only the field. When a future change alters the §7.2 shape, bump `SCHEMA_VERSION` and branch on the stored value in `getState()` to migrate old records.
- **All Gmail DOM automation is isolated to one module.** `src/lib/schedule/gmail-recipe.ts` is the *single point of failure* for Gmail UI changes — every selector and the synthetic-event recipe live there and nowhere else. Its header points to the canonical references: `research/scheduled-send-api-spike.md` (Verification section, preset path), `research/pick-date-time-probe.md` (custom path + relabel-target dump), and `research/send-button-probe.md` (the §5.5.1 regular-Send button + the whole-gesture / capture-phase / replay facts). Run the matching probe to rediscover selectors when Gmail breaks: Schedule Send → the spike/pick-date-time probes; the regular-Send guard → `send-button-probe.js` (`discover()` → `watch()` → `armBlockGesture()` → `testReplay()`). When either breaks post-launch, fix it here.
- **§5.5.1 intercepts the WHOLE Send gesture, not one event.** The probe proved Gmail finalises the send on a *later* event of the gesture (pointerup/click), so the regular-Send guard captures pointerdown+mousedown+pointerup+mouseup+click **and** ⌘/Ctrl+Enter — a single-event hook leaks (the one-shot `armSuppress` "sent anyway" that the gate caught). It only ever `preventDefault`s on a *certain* violation (decided synchronously off the cached config); everything else falls through to native Send. Do not "simplify" it to a click-only listener.
- **React inside Shadow DOM (the §5.3 modal).** Quirks, encoded so they aren't rediscovered: (1) `createRoot` must target an element **inside** the shadow root, not the host — since React 17 event delegation roots at the `createRoot` container, so rooting it in the shadow tree is what makes synthetic events fire for shadow-rendered controls; (2) page CSS does not cross the shadow boundary, so the modal's styles **must** be injected into the shadow root (`schedule-modal/styles.ts`) — the upside is hard isolation in both directions (Gmail can't break us, we can't leak into Gmail); (3) keep one host at a time — re-opening removes the previous host so a stale React root can't linger. See `src/content/schedule-modal/mount.tsx`.
- **The content script now bundles React** (~190 kB `jsx-runtime` chunk) because the §5.3 modal is React and is imported by `content-script.ts`. It loads on every Gmail page. Acceptable for v1/MVP; a later optimisation is to lazy-load the modal bundle only when the user actually triggers Schedule Send. Flagged, not yet done.

## Source-of-truth documents

Read these before making non-trivial decisions — they encode constraints that aren't visible in the code:

- `OutboxIQ_PRD.md` — full spec. **§11 "Out of Scope: Do Not Build"** is unusually load-bearing: it explicitly forbids 20 plausible-sounding features (email tracking, AI rewriting, analytics dashboards, separate scheduled-emails view, multi-account, holiday awareness, Firefox support, telemetry, etc.). Treat it as a hard constraint, not a suggestion — do not infer or add features outside it. **§13 "Premium v1 Scope"** is the opposite of §11: it holds the *preserved, intact* design for the backend-dependent features (Unschedule-on-Reply, the backend service, Premium Option-B OAuth) that are **tier-gated to Premium v1, deferred from Free v1, NOT deleted**. Don't conflate §11 (never build) with §13 (build, in the Premium tier). The §1 amendment is the tier-split overview.
- `PRE_LAUNCH_CHECKLIST.md` — **Free v1** public-launch obligations (Free-v1 OAuth Production + a *restricted-scope* CASA assessment, Privacy/Terms Free-v1 drafting, brand work, Web Store submission, pre-launch probes). `PREMIUM_LAUNCH_CHECKLIST.md` — **Premium v1** launch gates (CASA **Tier 2**, backend infra, online→offline OAuth switch, backend Privacy/ToS, Free→Premium migration, billing). Anything you'd otherwise spend time on for "launch readiness" belongs in one of these, not in feature code — pick the tier.
- `notes/session-7-summary.md` — latest session state and what the next session should start with (earlier summaries: `session-1`…`session-6`).
- `notes/owner-decisions-log.md` — running record of moments where owner/PM input materially changed the build's trajectory, with honest counterfactuals. Portfolio + coaching artifact; **must be maintained every session** (see "Working in this repo").

## Architecture

Two independently-deployed components, kept deliberately small:

### `extension/` — Chrome MV3 (does almost everything)

Local-first by design. All recipient-timezone detection, scheduling UI, working-hours logic, and storage runs in the browser. The extension **enhances** Gmail's native Schedule Send rather than replacing it — the native dropdown is rebranded and the native modal is enriched, but Gmail's interaction patterns and the Scheduled label are preserved (PRD §8.1 "native feel over branded feel"). Planned layout: `src/background/` (service worker, OAuth, backend messaging), `src/content/` (Gmail DOM injection), `src/pages/{onboarding,settings}/`, `src/lib/` (shared utilities), `public/icons/`. **In Free v1 the service worker's OAuth is the `access_type=online` extension-side flow (see "Google Cloud / OAuth"); "backend messaging" is Premium-v1-only** — Free v1 has no backend.

### `backend/` — EU-hosted relay (Premium v1 only; exists for exactly one reason)

> **Premium v1 scope (2026-05-17, owner-directed — Entry 32).** The `backend/` component is **not part of Free v1** — Free v1 ships extension-only. Everything in this block describes the **Premium v1** backend (PRD §13). It is **not the next work**; it is a separate later track gated by `PREMIUM_LAUNCH_CHECKLIST.md`. The "exists for exactly one reason" framing, the Maps-removal narrowing (Entry 26), and the OAuth-token-management refinement (Entry 31) all **carry forward to the Premium v1 backend intact** — preserved, not reopened.

The backend is **not** a general server. It exists for one specific server-side concern and nothing else:

1. **Unschedule-on-Reply relay** — subscribes to Gmail push notifications via Google Cloud Pub/Sub and cancels scheduled emails when a reply arrives (PRD §5.6, §7.3).

> **Google Maps APIs were removed from product scope (2026-05-16, owner-directed — not deferred, removed).** The former second purpose (Maps Geocoding/Time Zone proxy for recipient-timezone resolution) is gone entirely: no Maps API key, billing, OAuth scope, or proxy endpoint, ever. Recipient timezone resolution is fully on-device (cache → People API → Workspace Directory → manual, cached forever). Rationale + counterfactual: PRD §5.4.1 amendment, §7.3.1, `notes/owner-decisions-log.md` Entry 26. Do not reintroduce a Maps dependency without re-opening that decision.

> **OAuth token management is part of the one purpose, not a second one (2026-05-17, owner-directed — pre-Session-8; PRD §7.5/§7.3.1 amendments, Entry 31).** Honest refinement of "exactly one reason": the backend's purpose is **Unschedule-on-Reply *and* the OAuth token management that enables it**. Refresh tokens live **only** on the backend (per-user-encrypted; server-side authorization-code exchange — "Option B"; PKCE-in-extension rejected). The backend owns `POST /auth/{exchange,token,revoke}`; the extension never stores a refresh token and fetches short-lived access tokens on demand; the Client Secret lives in backend env only. This is **infrastructure the one purpose structurally requires** (§5.6 needs backend-initiated Gmail calls when the extension isn't running ⇒ a backend refresh token ⇒ the backend runs the exchange), **necessarily reused** by the extension's other Google calls (one OAuth grant per user) — *not* a second purpose and *not* a reversal of the Entry-26 single-purpose narrowing. The scope test is unchanged: not-Unschedule-and-not-its-OAuth-plumbing does not belong here.

Do not add other endpoints, telemetry sinks, user-data storage, or "while we're here" features. If you find yourself wanting to, that's a signal to push the logic back into the extension or to stop.

**Data rules:** no email content is ever stored. Stored data is limited to user email, encrypted OAuth refresh token (per-user keys), and active scheduled-message records. EU region (Frankfurt/Amsterdam) for GDPR data-residency.

## Locked tech decisions

Already decided — do not re-litigate without explicit user input:

- **Extension:** React + Vite, **TypeScript**, Manifest V3. Build tooling is **Vite + `@crxjs/vite-plugin`**, chosen for content-script hot-reload during development; we explicitly accept that CRXJS is a community plugin that may occasionally lag Vite releases. The TypeScript language layer and the CRXJS build tool were locked on 2026-05-15 — like the other items here, do not re-litigate the language or build-tool choice without explicit user input. (Rationale for TypeScript: the project owner is non-technical and relies on Claude Code for implementation, so compile-time error-catching and explicit code architecture matter more here than for a team that debugs runtime issues fluently.)
- **Backend (Premium v1 only):** Node + Hono on Fly.io, EU region. **These locks remain locked — they are *Premium v1* tech locks, not Free v1** (Free v1 has no backend; Entry 32). Do not read their presence here as Free v1 needing a backend, and do not re-litigate them for the eventual Premium v1 build.
- **Database (Premium v1 only):** Supabase, EU region. Same scoping as the backend lock above — locked, Premium-v1-tier.
- **License:** All-rights-reserved / proprietary during v1 development (changed from MIT on 2026-05-15 when the repo went public for portfolio visibility — public to read, no reuse rights granted). The eventual public-launch commercial model (open-source, royalty-on-commercial-use, dual-license, source-available) remains a pre-launch decision. Do not "restore" MIT assuming it's the locked default — the all-rights-reserved choice is deliberate.
- **Telemetry:** zero. No analytics SDKs, no usage events, no opt-in hedge. Success metrics are qualitative for v1.
- **Privacy/ToS docs:** drafting deferred; will live in `docs/` and be served from GitHub Pages.
- **Brand assets:** placeholder "OQ" mark during dev; real brand work happens just before Web Store submission.
- **Brand-independent stable identifiers (locked 2026-05-17, Session 7).** The product brand name may change before launch; these technical identities are deliberately decoupled from it and **must NOT be renamed on a rebrand** (renaming buys nothing and breaks identity/data/tokens): the **extension ID** (from the manifest `key`, not `name` — `dicnmcmhapcfceodecocnkaacjdpplnm`), the **OAuth Client ID** + `chromiumapp.org` **redirect URI**, the **GCP project ID** (`outboxiq-dev`) and any future Fly/Supabase resource names, and the **storage keys** (`outboxiqState`/`outboxiqOnboardingDraft` in `src/lib/constants.ts`). A rebrand = display copy + assets + a deliberate PRD/locked-copy amendment (the verbatim `"…powered by OutboxIQ"` label + onboarding privacy copy are spec text) — nothing more. Rename-proofing the brand-named Privacy/ToS/Pages URLs is a tracked launch-blocker (`PRE_LAUNCH_CHECKLIST.md` "Naming / rebrand readiness"). A `PRODUCT_NAME`-constant centralisation of incidental copy is an explicitly-deferred optional future task — do not treat its absence as drift.

## Locked product decisions

Product/UX decisions locked by the owner — do not re-litigate without explicit user input:

- **§5.5 working-hours / absolute-limits enforcement is a soft warning, not a hard block and not a silent auto-snap (locked 2026-05-16, Session 5).** When a user tries to schedule (or send) an email at a time that violates their configured working hours or absolute limits, show a soft-warning modal that names the violation in plain language (e.g. "You set 7:00 AM as your earliest send time. This email is scheduled for 3:33 AM. What would you like to do?") and offers exactly three explicit choices: **(1) Proceed anyway** (honour the user's specified time, overriding the limit just this once); **(2) Snap to [the violated boundary]** (e.g. 7:00 AM, or the relevant working-hours/absolute boundary for the rule that was violated); **(3) Cancel** (close the modal, leave the user in compose to adjust). Rationale: a hard block is too paternalistic (legitimate overrides exist, e.g. a client in another timezone) and damages trust; a silent auto-snap is surprising and violates §8.3 "sensible defaults, easy overrides" (the user must always know what's happening to their email); a soft warning gives agency — it acknowledges the violation, surfaces options, and requires an explicit choice, matching the PRD's own §5.5 modal pattern. The **same pattern applies to both** working-hours violations and absolute-limits violations. If **both are violated at once, surface the absolute-limit violation** (it is the harder of the two constraints). Do not replace this with a block, an auto-snap, or a 2-option variant in any future session.

- **Warnings fire for unintended actions, not the product's core use case (locked 2026-05-16, Session 5.5).** The §5.5 trigger is **split**: **Schedule Send warning = absolute limits only**; **Regular Send (§5.5.1) warning = working hours + absolute limits**. Reasoning: deliberately *scheduling* outside one's working hours to land in a recipient's window is exactly what OutboxIQ is *for* — warning on it trains users to dismiss the modal and guts its value for the absolute-limit case where it matters. An *immediate* off-hours Send, by contrast, is plausibly unintended, so §5.5.1 warns on both. The §5.5 calc (`working-hours.ts`) still computes **both** rule types unconditionally — **do not delete the working-hours branch**; it is consumed by §5.5.1 (Session 5.6) and §5.3.5 recipient-optimization (Session 6, advisory input only). The **default 9 AM working-hours start is retained**: under this split it does *not* nag Schedule Send, but it *does* drive the §5.5.1 regular-Send prompt (clicking Send at 8:30 AM → "you're sending before your stated start time — schedule for 9 AM instead?"). That prompt is **signal, not noise** — a user whose real pattern differs adjusts their working hours in onboarding. Do not re-litigate the default or the split.

- **§5.5 snap-target resolution (locked 2026-05-16, Session 5).** "Snap to [boundary]" resolves differently per rule type. **Working-hours violation** → the §5.5.3 next-working-day algorithm (next configured working day at that day's working-hours start). **Absolute-limit violation** → the absolute boundary (`absoluteEarliest`/`absoluteLatest`) **on the same calendar day the user originally picked** — *not* next-working-day. Reasoning: absolute limits are *clock-time* rules ("never send before 7 AM / after 7 PM in my local time"), not day-of-week rules; the user chose their target day deliberately when setting the custom time, so the snap fixes only the clock-time violation while honouring the day-of-week intent. Consistent with PRD §5.1.3's "Earliest I'd ever send an email" framing.
- **§5.5 absolute snap may land on a non-working day — intentionally permitted in v1 (locked 2026-05-16, Session 5).** Because an absolute snap keeps the user's chosen day (above), it can land on a non-working day (e.g. Sat 7:00 AM with Mon–Fri working hours). This is **deliberate**: the user's explicit day choice overrides the working-hours *soft* preference (working hours are a soft preference; absolute limits are the hard rule being enforced here). Do **not** "fix" this in v1 by re-snapping to a working day or chaining a second warning. A v2 enhancement *could* surface a secondary informational note in the modal ("this lands outside your working hours, but within your absolute limits") — **deferred, not v1 scope; do not implement.**
- **§5.5 past-`after-latest`-snap forward-roll (locked 2026-05-17, Session 7; refines — does not overturn — the two bullets above).** The "absolute snap = the violated boundary on the same calendar day" rule is correct only when that day is a *future day the user picked* (§5.3 Schedule Send). For an **`after-latest`** violation the same-day ceiling is **in the past** when there is no picked future day — the §5.5.1 *regular Send* trigger (requested time *is* "now", so "now is after the ceiling" ⇒ today's ceiling already elapsed) — or when a Schedule-Send pick is later-today while already past the ceiling. Gmail rejects a past time ("Invalid time"); found by the Session 7 Phase 1 hands-on smoke (Test G), **default-reachable** (Send after the default 7 PM `absoluteLatest`). Locked resolution: an `after-latest` snap not safely in the future **rolls forward to the next working morning** (soonest configured working day strictly after now, at its start; **falls back to next calendar day at `absoluteEarliest`** if zero working days). Scoped to `after-latest` **only** — every other snap is provably already strictly future, so the locked behaviour above (incl. the intentional non-working-day landing of a *future* picked absolute day) is untouched while still future. Owner chose "next working morning" over "tomorrow at earliest" for consistency with the §5.5.2 "next working window" copy and the working-hours `after-end` behaviour. Implemented as a pure, time-aware `ensureFutureSnap()` applied by both the §5.5.1 and §5.3 consumers; `checkWorkingHours` stays unchanged/Date.now-free. PRD §5.5.3 amended; `notes/owner-decisions-log.md` Entry 28. Do not re-collapse this into "always same-day" or delete the forward-roll.
- **§5.5.1 regular-Send-button trigger — deferred (Session 5 owner decision).** §5.5 enforcement this session guards only times the user explicitly picks via OutboxIQ's Schedule Send modal. Intercepting Gmail's *primary Send* button when the current time is outside working hours (PRD §5.5.1 first bullet) is its own future session — far more invasive and a direct §5.2.3 ("never block native Gmail") risk. The PRD §5.5.1 first bullet remains spec; it is simply not yet implemented.
- **§5.5.2 auto-suppress footer checkbox — dropped for v1 (Session 5 owner decision).** The locked soft-warning pattern requires an explicit choice every time, so the PRD §5.5.2 "Always schedule for next working day when outside hours" suppression checkbox is not built; PRD §5.5.2 is amended accordingly. Consequence to note (and not "fix"): the `alwaysScheduleOutsideHours` field in the §7.2 storage schema and its planned §5.8 Settings toggle are **inert in v1** by design — left in place (schema stability) but unused. Do not wire them to silent auto-snap behaviour; that is exactly the "surprising silent shift" the locked decision rejects.

## Gating constraint: Gmail scheduled-send spike — CLEARED (2026-05-15)

The Gmail API does not expose third-party scheduled sends directly, so a half-day technical spike was required before any Schedule Send / Pub/Sub / recipient-cache code. **That spike is complete and verified** (`research/scheduled-send-api-spike.md`): Approach C (UI-automate Gmail's own Schedule Send) confirmed end-to-end, and the API cancel path (`messages.list?q=in:scheduled` → `messages.trash`) verified. Schedule Send, backend Pub/Sub, and recipient-cache work are now **unblocked**. Still open from the spike (not blocking, but do them at the right time): verify the "Pick date & time" custom path when §5.3 begins, and Workspace compatibility before public launch (tracked in `PRE_LAUNCH_CHECKLIST.md`). The onboarding flow (PRD §5.1), the spike-independent starting point, is now implemented — see Repository status.

## Google Cloud / OAuth

Fresh GCP project, **just-in-time setup** — wire up Cloud APIs only when a feature actually needs them. OAuth stays in **Testing mode** with an explicit test-user allowlist during development; switching to Production for **Free v1** still requires *a* CASA security assessment because Free v1 keeps the **restricted** Gmail scopes (`gmail.compose`, `gmail.modify`) — **but not necessarily *Tier 2***: Tier 2 is driven by *backend + restricted scopes*, and Free v1 has no backend, so Free v1 is plausibly **Tier 1** (tier to confirm with Google's CASA docs — itself a Free-v1 pre-launch task). The full **Tier 2** assessment moves to `PREMIUM_LAUNCH_CHECKLIST.md`. Do not read the split as "Free v1 needs no assessment" — restricted scopes always require one before public Production. (Both checklists track their tier's gate.)

**Status (Session 7 — now ACTIVE, no longer purely just-in-time):** GCP project **`outboxiq-dev`** exists; **Gmail + People + Calendar APIs enabled**; OAuth consent screen configured **External / Testing mode**; **2 test users** (`fenil.h.dedhia@gmail.com` + a throwaway `anonymoustiel@gmail.com`); **4 scopes** requested — `gmail.compose`, `gmail.modify`, `calendar.settings.readonly`, `contacts.readonly` (**not** `directory.readonly` — Session 8 additive). A **Web-application** OAuth client was created (owner override of the prompt's literal "Chrome extension" type — Entry 29). **It is retained for Free v1**: the *Entry-29 reason* was the (now Premium-v1) refresh-token need, but the **load-bearing reason it is also correct for Free v1 is multi-Google-account UX** — a Chrome-extension client drives `chrome.identity.getAuthToken`, which silently uses the profile's primary account and **cannot let a multi-account user choose which Google account to authorize**; `launchWebAuthFlow` on the Web-app client fixes that *and* is the same client Premium v1 reuses (flip `access_type` online→offline, no client swap). Entry 32 records the credit split (Claude initially leaned toward switching to a Chrome-extension client; owner's "do we lose anything?" forced the multi-account re-examination). The extension ID is **pinned stable** via a manifest `key` (`dicnmcmhapcfceodecocnkaacjdpplnm`; private key `extension-key.pem`, `*.pem`-gitignored) so the OAuth redirect URI `https://<id>.chromiumapp.org/` never drifts. The **Client ID lives in a typed `src/lib/oauth-config.ts`** (public-by-design for installed clients — **not** an env var, **not** the manifest `oauth2` key, which only drives the unused `getAuthToken` path); the Client *secret* is held by the owner outside the repo. Verified hands-on (zero-code authorize URL): both test accounts reach the consent screen, see all 4 scopes, return `?code=` (redirect matches). Consent-screen Privacy/ToS URLs are intentionally left blank (Google requires an owned authorized domain even there; rename-proof legal-doc URL is a tracked pre-launch item — see PRE_LAUNCH "Naming / rebrand readiness").

**Token architecture — now TWO models, one per tier (2026-05-17, owner-directed — Entry 32; PRD §7.5 + §13.3 amendments). The Web-application OAuth client is the locked choice for BOTH tiers** (not Chrome-extension/`getAuthToken` — multi-account UX, above).

- **Free v1 (IMPLEMENTED, Session 8) — implicit grant, no secret, no backend.** *(Session-8 refinement of the Entry-32 wording: the tier-split draft said "code + Client Secret exchange"; the owner chose the **implicit grant** at the Entry-19 review — simpler, fewer failure modes, ships zero secret. PRD §7.5/§6.5 amended per Entry-6; owner-decisions-log Session 8.)* The SW (`src/background/oauth.ts`) runs `launchWebAuthFlow` with **`response_type=token`** against `accounts.google.com`; Google returns an **access token only** (~1 h; **no refresh token — inherent to the implicit grant**) in the redirect fragment. **No code exchange, no token endpoint, no Client Secret anywhere in the extension.** A CSRF `state` is round-tripped + verified. The token is cached in `chrome.storage.local` key `outboxiqAuth` (`src/lib/auth-token.ts`; separate from `outboxiqState`, extension-private — §6.5) with expiry+scopes; `getAccessToken({interactive})` is the single entry point (valid cache → silent re-auth → interactive → typed `needs_interactive`; never throws — §5.2.3/§6.7). Manifest now has `identity` + `https://www.googleapis.com/*`. **§6.5 holds with NO secret exception for Free v1** (there is no secret to except).
- **Premium v1 (separate later track) — Entry-31 Option B, preserved and still locked.** **Refresh tokens live ONLY on the backend, per-user-encrypted, server-side authorization-code exchange (Option B); PKCE-in-extension (Option A) stays rejected.** This is **not reopened by the tier split** (Entry-4 discipline) — it was correct given the then-true assumption that Unschedule-on-Reply was in the only tier; it remains the binding Premium-v1 design, preserved verbatim in **PRD §13.3**. The flip from Free v1 is purely `access_type` online→offline on the *same* client + adding the backend `/auth/{exchange,token,revoke}` plumbing. **Do not relitigate Option A vs B for the Premium build, and do not "restore" Option B onto Free v1** — Free v1's simpler model is the deliberate Entry-32 decision, *not* a reversal of Entry 31.

**Free v1 roadmap (supersedes the old 3-session backend-first plan):**
- **Session 8** — **extension-side** OAuth (`launchWebAuthFlow` + **implicit grant `response_type=token`**, no code exchange, access-token storage; **no backend**) ✅ *done*; + Calendar API (user timezone, §5.1.3) + People API + Workspace Directory + recipient cache + the `resolveRecipientTimezone()` contract *(Phase 3, in progress)*.
- **Following Free v1 session(s)** — §5.3.5 Optimize-for-recipient UI in the §5.3 modal; then Free v1 pre-launch hardening + the two Gmail-API probes (`PRE_LAUNCH_CHECKLIST.md` "Pre-launch probes") + naming/positioning.
- **Premium v1 (separate, later — NOT the next work)** — backend skeleton (Fly.io EU, Hono, Supabase EU, per-user-key encryption) + Option-B server-side exchange + Unschedule-on-Reply. Gated by `PREMIUM_LAUNCH_CHECKLIST.md` / PRD §13.

> **Tracking marker (Entry-6 discipline — do not lose this):** PRD §5.1.3
> ("Detected from your Google Calendar settings") is deliberately **NOT yet
> amended** — the Calendar path is unbuilt, so the spec does not yet
> diverge. **The Free v1 session that wires the Calendar timezone source
> (now Session 8, per the roadmap above — moved earlier by the tier split,
> was Session 9) MUST amend PRD §5.1.3** when the live source replaces the
> browser-only fallback. This marker exists so the amendment is not
> forgotten when Calendar ships (the risk the owner flagged at Session-7
> close).

> **Stale code comment to fix in Session 8 (flagged, not a doc):**
> `extension/src/lib/oauth-config.ts` comments still float "PKCE vs
> backend-side exchange … Phase 3 decision" / "via PKCE" as open. That
> framing is now **doubly** stale — Free v1 uses neither (it is
> `access_type=online`, extension-side exchange, **no refresh token**);
> Option B is the **Premium v1** design (PRD §13.3). Comment-only, no
> logic, not launch-affecting; correct it to the Free v1 reality (with a
> Premium-v1 pointer) when Session 8 first touches that file (out of scope
> for this documents-only tier-split change).

## Working in this repo

- The user (Fenil) is non-technical for implementation details. Narrate decisions in plain English, and confirm before destructive or hard-to-reverse actions.
- When a feature touches privacy, OAuth scopes, data residency, or anything the PRD §11 list could conceivably bear on, re-read the relevant PRD section before writing code rather than working from memory.
- If you add tooling (package manager, build, lint, test, deploy), record the resulting commands in this file in a new "Commands" section so future sessions don't have to rediscover them.
- **Session close-out obligation.** At every session end, alongside updating the session summary, append to `notes/owner-decisions-log.md` either a new entry (using that file's fixed structure) for any moment where owner/PM input materially changed the build's trajectory, or the single line `Session N — no entries this session.` if none qualified. "No entries" is a legitimate and common outcome — do not manufacture entries to fill the log. The honesty rule in that file is binding: counterfactuals must be accurate not flattering, and owner judgment that was wrong or incomplete is logged too.
