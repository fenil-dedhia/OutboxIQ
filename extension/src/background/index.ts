// OutboxIQ service worker (PRD §7.1.1) — toolchain scaffold.
//
// Real responsibilities (OAuth token management, backend messaging, message
// passing to the content script) are added just-in-time with the features that
// need them. Nothing here yet beyond proving the MV3 service-worker entry
// point builds and loads.
console.info("[OutboxIQ] service worker loaded (scaffold)");

export {};
